query getMyGroups($id: ID!) {
    groups(where: { members: { id: $id } }) {
        id
        name
        open_slots
        max_age
        min_age
        booking_status
        description
        leader {
            id
        }
    }
}
